import { ModelTierCategory } from './model-tier-category';
import { MockTierCategory } from './mock-tier-category';

export class ModelTier {
    constructor(
        id: number, 
        title: string,
        description: string, 
        category: ModelTierCategory
        ){
        }
}
