image: node

before_script:
  - pwd

pages:
  script:
    - cd public-website
    - npm --version
    - date
    - npm run generate
    - date 
    - du -sh public/
    - mv -r public ../public
    - ls -a 
    - pwd 
    - cd .. 
    - ls -a 
    - pwd
  artifacts:
    paths:
      - public

